# GIM Viewer 性能特征分析

> 采集日期：2026-09-01（真实 Tauri/CDP 灰度矩阵完成于 2026-09-01）
> 目的：记录 ProjectLoadSession 竞态修复、Performance Infrastructure、线路批量读取和 Line Parser Worker v1 的真实样本证据。
> 结论先行：六个真实线路样本均在真实 Tauri 中以 `worker:true` 完成冷启动解析，36/36 次运行成功且语义统计一致；线路 Worker 通常只耗时 47–495 ms，当前主耗时是 Rust 解包、批量输入、SQLite 入库和地图渲染。真实 `invokeTimed`、WebView Long Task、WebView 进程工作集已采集；本轮没有 IFC/Fragments 路径，因此 Fragments Cache 仍不提升为默认能力。

## 0. 本轮 Performance Infrastructure + Line Worker v1

### 0.1 实现边界

本轮只实现以下内容：

- `perfTimings` 增加独立的性能 session（`sessionId` + generation/project/source 身份）；迟到 span、invoke 和 Long Task 不会写入新工程。
- 所有 Tauri bridge command 统一经过 `invokeTimed`，按 command 统计次数、请求/响应估算 bytes、total、p50、p95、max 和 failures；诊断快捷键导出的 `timings` 已包含这些数据。
- WebView 安装 Long Tasks observer，统计 count、total blocking time（`max(0,duration-50ms)`）和 max；`perfReset` 重建 observer，旧回调按 session 丢弃。
- 线路文本文件按 **最多 256 个文件 / 8 MiB** 分批调用 `batchReadCachedFiles`；路径匹配保持分隔符和大小写不敏感。非语义大二进制（例如 STL）只传路径元数据，不复制到 Worker。
- Line Parser Worker v1 只接受 `{path, bytes}` 和可序列化的 `ProjectLoadSession` 快照，在同一 Worker cache 上完成 `buildLineGimGraph` 与 `parseLineAttributes`；ArrayBuffer 使用 transferable。Worker 结果提交前和清理时均做 session 校验。
- Worker 不可用时保留同步核心回退，便于浏览器/测试环境运行；这不是生产路径的性能结论。

暂不包含 IFC Semantic Worker、DEV→GLB Worker、小文件 LRU、Fragments Cache 默认开启或 IFC Spatial Index 重构。

### 0.2 六个真实线路 GIM 的 Node/jsdom 历史对照

采集器：`tmp/performance-line-worker.test.ts`；结果：`tmp/performance-line-worker-results.json`。这是上一轮的 Node/jsdom 核心对照，不替代本轮真实 Tauri 数据。样本目录来自 `demo/`，没有生成缩小或合成样本。`旧总耗时` 是旧 File API parser 的图构建+属性解析；`新输入` 是批量输入准备；`Worker 总耗时` 是 Worker 核心内部 decode+graph+attributes 的总和；`新端到端` 为新输入+Worker 总和，不含 UI 地图渲染和 SQLite 入库。

| 样本 | 文件/文本文件 | 文本字节 | 估算批次 | 旧总耗时 ms | 新输入 ms | Worker decode/graph/attrs/总 ms | 新端到端 ms | 端到端倍率 | 解析倍率 | 采样 RSS 峰值 MB | 语义一致 |
|---|---:|---:|---:|---:|---:|---|---:|---:|---:|---:|:---:|
| demo-line1 | 12,372 / 12,290 | 35,728,253 | 49 | 50,604.07 | 9,486.80 | 62.35 / 123.89 / 90.18 / 310.57 | 9,797.37 | 5.2× | 162.9× | 293.76 | 是 |
| line02 | 22,640 / 22,520 | 65,794,426 | 88 | 1,641.38 | 1,591.94 | 65.26 / 196.06 / 145.34 / 439.23 | 2,031.17 | 0.8× | 3.7× | 593.14 | 是 |
| line03 | 18,533 / 18,442 | 35,752,660 | 73 | 87,690.58 | 6,041.24 | 51.05 / 223.49 / 71.65 / 394.16 | 6,435.40 | 13.6× | 222.5× | 905.56 | 是 |
| line04 | 1,394 / 1,353 | 7,101,357 | 6 | 6,216.71 | 1,295.80 | 5.52 / 14.45 / 13.76 / 35.78 | 1,331.58 | 4.7× | 173.7× | 880.21 | 是 |
| line05 | 15,480 / 15,359 | 55,509,705 | 60 | 71,066.88 | 12,112.55 | 76.19 / 132.01 / 99.10 / 326.76 | 12,439.31 | 5.7× | 217.5× | 708.19 | 是 |
| line06 | 2,508 / 2,459 | 13,394,902 | 10 | 10,506.76 | 1,958.60 | 15.49 / 14.06 / 8.80 / 42.71 | 2,001.31 | 5.2× | 246.0× | 747.30 | 是 |
| **合计/观测峰值** | **72,927 / 72,423** | **213,281,303** | **286** | **227,726.38** | **32,486.93** | **275.86 / 703.96 / 428.83 / 1,549.21** | **34,036.14** | **6.7×** | **147.0×** | **905.56** | **6/6** |

倍率仅用于描述本次采集的数量级，不作为发布 SLA：旧 parser 和六个样本在同一 Vitest worker 中顺序执行，受到 Windows 文件系统缓存、GC、进程 RSS 保留和机器负载影响；本次重跑中 `line02` 的旧基线明显受热文件系统缓存影响，故出现端到端倍率低于 1。应在固定 Tauri 机器上重复至少三次，以中位数/P95 建立门槛。

### 0.3 本轮真实 Tauri/CDP 灰度结果

采集器：`tmp/collect-tauri-perf.ps1`；原始诊断：`tmp/tauri-perf-20260901/raw/` 和 `*.full.json`；汇总：`tmp/tauri-perf-20260901/summary.md`。在同一个真实 Tauri Dev WebView 中，对 `demo/line01.gim`–`line06.gim` 各执行冷启动 3 次（每次先删除该项目 SQLite 缓存）和缓存命中 3 次，共 36 次。表中数值是每组三次的中位数；P95 按三次中的最大值计算。`ready` 使用“面板+地图渲染”结束点，避免触发诊断快捷键带来的等待污染；RSS 是 Tauri 进程树工作集峰值采样。

#### 冷启动（缓存无效）

| 样本 | ready 中位/P95 ms | 解压 ms | 输入 ms | Worker ms | batch 次数 | batch bytes | batch 总/P95/最大 ms | SQLite ms | 地图 ms | Long Task 次数/阻塞/最大 ms | RSS 峰值中位/最大 MB |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| line01 | 93,373 / 94,008 | 59,452 | 13,706 | 256 | 49 | 37,045,332 | 13,604 / 319 / 353 | 10,256 | 9,496 | 21 / 11,321 / 9,496 | 1,729 / 1,743 |
| line02 | 193,610 / 221,309 | 113,133 | 25,045 | 495 | 90 | 68,207,878 | 24,866 / 320 / 364 | 18,059 | 34,338 | 21 / 36,939 / 34,337 | 2,318 / 2,322 |
| line03 | 152,668 / 153,533 | 99,900 | 19,357 | 317 | 70 | 37,646,302 | 19,220 / 311 / 375 | 7,743 | 24,077 | 19 / 25,821 / 24,077 | 1,933 / 1,952 |
| line04 | 9,086 / 9,859 | 5,406 | 1,743 | 47 | 6 | 7,246,328 | 1,728 / 394 / 394 | 1,578 | 119 | 5 / 356 / 159 | 1,262 / 1,340 |
| line05 | 118,729 / 121,052 | 76,796 | 17,257 | 345 | 62 | 57,155,726 | 17,129 / 324 / 364 | 13,431 | 11,168 | 20 / 13,555 / 11,168 | 1,909 / 1,915 |
| line06 | 16,549 / 17,752 | 11,176 | 2,998 | 74 | 10 | 13,658,387 | 2,977 / 365 / 365 | 1,724 | 423 | 5 / 880 / 423 | 1,312 / 1,494 |

冷启动六个样本的 `worker:true` 均成立；`read_cached_entry.count=0`，线路语义输入没有回退到单文件 IPC。`batch_read_cached_files` 的请求使用已知路径字节量估算，响应 envelope 为二进制并精确计数，全部 `failures=0`。

#### 缓存命中（二开）

| 样本 | ready 中位/P95 ms | SQLite/输入/Worker | 地图渲染 ms | Long Task 次数/阻塞/最大 ms | RSS 峰值中位/最大 MB |
|---|---:|---|---:|---:|---:|
| line01 | 10,764 / 11,075 | 不读取/不启 Worker | 7,579 | 3 / 7,716 / 7,580 | 1,082 / 1,139 |
| line02 | 30,821 / 32,368 | 不读取/不启 Worker | 24,735 | 5 / 25,058 / 24,735 | 1,237 / 1,627 |
| line03 | 21,354 / 22,394 | 不读取/不启 Worker | 19,272 | 5 / 19,682 / 19,272 | 977 / 1,232 |
| line04 | 903 / 1,029 | 不读取/不启 Worker | 93 | 2 / 60 / 97 | 864 / 890 |
| line05 | 11,055 / 12,892 | 不读取/不启 Worker | 7,280 | 5 / 7,668 / 7,280 | 1,405 / 1,415 |
| line06 | 971 / 1,120 | 不读取/不启 Worker | 307 | 2 / 450 / 307 | 1,035 / 1,176 |

缓存命中时诊断改为按 `currentProjectId` 查询，避免把“最新项目”与当前 timings 串在一起；缓存路径的图、属性恢复和地图渲染均在同一 session 下完成。

### 0.4 IPC、Long Task 和内存的测量边界

| 指标 | 本轮状态 | 解释 |
|---|---|---|
| Tauri IPC command/count/bytes/total/p50/p95/max/failures | **已在 36 次真实 Tauri 运行采集** | 冷启动 batch 次数为 6–90、每批 P95 约 311–394 ms（line02 总输入 IPC 中位 24.9 s）；`read_cached_entry=0`，失败数为 0。普通对象仍标 `bytesMeasured=false`，不会为探针重复 JSON 序列化。 |
| WebView Long Task count/blocking/max | **已在真实 WebView 采集** | 冷启动最大长任务基本对应地图渲染（119–34,338 ms），Worker 核心没有形成秒级主线程任务；Long Task API 统计口径为 `max(0,duration-50ms)`。 |
| 峰值内存 | **Tauri 进程树工作集已采样** | 冷启动峰值约 1.26–2.32 GB，热启动约 0.86–1.63 GB；这是进程树工作集，不等同 JS heap/RSS 单进程，仍需在目标硬件建立门槛。 |
| Worker parse 时间 | **生产 Worker 已测** | 六个冷启动均 `worker:true`，Worker 解析中位 47–495 ms；缓存命中预期不启 Worker，直接恢复 SQLite 图/属性。 |

### 0.5 结果解释与下一轮门槛

1. 解析核心不是当前线路的主要瓶颈：六个样本冷启动均 `worker:true`，Worker 中位 47–495 ms；对应输入准备中位 1.7–25.0 s，SQLite 中位 1.6–18.1 s，地图渲染中位 0.1–34.3 s。Worker 已把纯解析 CPU 从 UI 主线程隔离，但输入、入库和地图仍会产生明显长任务。
2. 批量策略在真实 Tauri 中把线路文本读取压缩为 6–90 次 batch IPC；每批 P95 约 311–394 ms，未出现 `read_cached_entry` 回退，说明当前 256 文件/8 MiB 限制有效，但 line02 的总 batch 等待已达到约 24.9 s，下一轮应优先优化批量 IPC/入库，而不是再优化 Worker。
3. 缓存命中不再读取原始 GIM，恢复图/属性后主要成本是地图渲染；line01/line02/line03 的热启动地图长任务仍达约 7.6/24.7/19.3 s，应优先做地图分块/虚拟化和渲染调度。
4. 当前证据不足以决定 Fragments Cache 默认开启：本轮没有 IFC→Fragments 首开/二开、命中/失败/回退数据，故继续保持灰度开关关闭。
5. 下一轮先优化线路输入批次与地图长任务；线路稳定后再进入 Substation Semantic Core。当前不直接扩展变电 IFC 改造。

## 1. 测试范围与方法

### 1.1 样本

使用 `demo/` 下已经解压的真实样本目录，未生成缩小版或合成数据：四个变电样本和六个线路样本。

| 样本 | 类型 | GIM 大小 | 解压文件数 | 解压字节数 |
|---|---|---:|---:|---:|
| demo-substation（substation01） | 变电 | 14,381,403 | 36,112 | 145,074,932 |
| substation02 | 变电 | 34,176,631 | 11,307 | 578,675,362 |
| substation03 | 变电 | 71,831,575 | 9,203 | 694,268,165 |
| substation04 | 变电 | 11,789,608 | 2,833 | 134,602,057 |
| demo-line1（line01） | 线路 | 5,652,236 | 12,372 | 47,644,841 |
| line02 | 线路 | 10,804,308 | 22,640 | 96,518,806 |
| line03 | 线路 | 7,308,960 | 18,533 | 58,728,224 |
| line04 | 线路 | 1,439,639 | 1,394 | 13,811,001 |
| line05 | 线路 | 8,674,709 | 15,480 | 77,304,869 |
| line06 | 线路 | 2,443,777 | 2,508 | 21,354,618 |

### 1.2 采集器

- `tmp/performance-characterization.test.ts` 使用磁盘文件驱动的 `CountingLazyFile`，包住现有 parser API 的 `text()`/`arrayBuffer()`，逐次记录路径、扩展名、字节数和读取耗时。
- 阶段计时覆盖 CBM 树、IFC 发现、FileDevRelation、变电 IFC 空间索引、STD/SLD、线路图、线路 FAM/DEV 属性和一次轻量几何发现。
- 每阶段记录 Node RSS、heapUsed、external；`duration >= 50 ms` 仅标为 **长任务候选**，不是 WebView Long Task API 的实测事件。
- 累计结果保存在 `tmp/performance-results.json`。线路样本在同一个 Vitest worker 顺序运行，RSS 可能包含前一个样本的未及时回收对象；变电样本逐样本单独运行。
- 本次运行环境：Node `v22.22.0`、Windows x64、Vitest + jsdom。

### 1.3 “读取次数”与 IPC 的边界

本采集器没有 Tauri runtime，也没有 `@tauri-apps/api/core.invoke`。因此表中的 `readCount/readBytes` 是**真实 parser 文件访问次数和字节数**，不是已经通过 IPC 观测到的调用次数。

在原生解压的 manifest-only 路径中，一个非空 `DiskBackedFile.text()`/`arrayBuffer()` 理论上可对应一次 `read_cached_entry` IPC；本轮线路解析已实际走 `batch_read_cached_files`，并在真实 Tauri 中确认冷启动没有 `read_cached_entry` 回退。缓存命中时直接从 SQLite 恢复图/属性，不读取原始 GIM；Fragments 缓存命中率和失败率仍未取得，后文单列为未测项。

## 2. 真实样本结果

### 2.1 总体读取与内存

十个样本合计：109,449 次逻辑读取、1,507,499,733 B（约 1.40 GiB），其中 82,356 个唯一路径、27,093 次重复读取，整体重复率 24.75%。按扩展名统计，IFC 仅 56 次读取却占 1,430,801,718 B（94.91%）；CBM/DEV/FAM 等小文件访问次数多，但字节量很小。

| 样本 | 逻辑读取次数 | 读取字节 | 重复率 | 观测 RSS 峰值 | 主要阶段（ms） |
|---|---:|---:|---:|---:|---|
| substation04 | 2,542 | 129,278,128 | 63.02% | 933.74 MB | CBM 254；IFC 空间 10,699；STD/SLD 828 |
| substation01 | 25,776 | 88,683,321 | 49.97% | 614.45 MB | CBM 103,390；IFC 空间 6,188；STD/SLD 228 |
| substation02 | 15,571 | 575,976,639 | 61.93% | 2,826.72 MB | CBM 49,333；IFC 空间 57,116 |
| substation03 | 1,627 | 652,318,471 | 49.72% | 3,223.57 MB | CBM 6,255；IFC 空间 35,111 |
| demo-line1 | 10,301 | 13,937,919 | 1.48% | 164.89 MB | 图 50,829；FAM/DEV 14,177 |
| line02 | 19,405 | 21,404,015 | 2.13% | 198.97 MB | 图 88,504；FAM/DEV 34,481 |
| line03 | 17,505 | 5,941,522 | 4.41% | 212.93 MB | 图 106,702；FAM/DEV 21,601 |
| line04 | 1,055 | 2,364,372 | 1.52% | 213.67 MB | 图 6,015；FAM/DEV 2,234 |
| line05 | 13,690 | 15,843,159 | 5.83% | 213.23 MB | 图 40,972；FAM/DEV 1,429 |
| line06 | 1,977 | 1,752,187 | 0.35% | 213.35 MB | 图 347；FAM/DEV 209 |

> 阶段时间是当前机器单次顺序采集值，不是 SLA；线路样本对系统负载和 GC 较敏感，应在固定机器上重复 3–5 次后用中位数做发布门槛。

### 2.2 读取慢点与重复读取

- 所有样本只观测到 23 次单文件读取超过 50 ms；绝大多数小文件的同步磁盘读取并没有达到秒级。
- 变电四样本重复率为 49.72%–63.02%，主要来自 CBM/DEV 解析对同一小文件的多次访问。线路重复率为 0.35%–5.83%，更适合使用阶段性批量读取。
- `DiskBackedFile` 已将 IFC 原文按文件逐条读取；空间索引阶段每个变电 IFC 只读一次（12、17、8、19 次），没有再次累积所有 IFC 原文的行为。

## 3. 瓶颈判断

### 3.1 磁盘 lazy read 是否成为瓶颈？

本轮线路证据显示，单批 IPC P95 约 311–394 ms，未出现单文件 `read_cached_entry` 回退；因此“每个小文件一次 IPC”的风险已被批量读取消除。但 line02 的 90 批累计等待约 24.9 s，说明批量 IPC 已经是输入阶段的主要成本之一，不能再用 Node 文件读取次数代替真实耗时。变电 IFC 的磁盘吞吐结论仍沿用历史样本：读取次数少，主要成本是空间语义解析。

未在本轮引入小文件 LRU；变电 CBM/DEV 小文件重复率仍是后续风险。若进入变电优化，建议沿用 `EntryReadCoordinator` 方向：

1. 对 CBM/FAM/DEV/PHM 等小于 256 KB 的文件使用 64–128 MB 有界 LRU。
2. 在一个解析阶段开始前收集确定的路径，优先调用 `batchReadCachedFiles`。
3. IFC/MOD/STL 不进入全局 LRU，继续逐文件释放原文。
4. 本轮 `invokeTimed` 已记录 command、字节数、耗时和 failures；后续可再增加 batch 命中率/缺失条目维度，但不要恢复逐文件 IPC。

### 3.2 主线程到底卡在哪里？

历史基线的主线程候选长任务集中在两类：

- 变电：`buildSubstationSpatialIndexFromFiles`。substation02/03 分别耗时 57.1/35.1 秒，RSS 达 2.83/3.22 GB；这说明“单个 IFC 解析后的语义对象/关系图”本身仍是主要内存成本，磁盘优先只解决了原文全量驻留，不能消除最终语义索引的成本。
- 线路（已由本轮修复）：真实 Tauri 六样本冷启动均 `worker:true`，Worker 中位 47–495 ms；Long Task 最大值由 SQLite/地图渲染产生（line02 冷启动地图约 34.3 s）。因此下一项瓶颈是输入批次、缓存写入和地图渲染，而不是 `buildLineGimGraph`/`parseLineAttributes`。

建议优先级：

1. 线路 CBM/FAM/DEV 解析已移入单个 Worker，真实 Tauri 已确认 Worker 使用和 transferable 输入；下一步优先削减 batch IPC 累计等待，并把地图渲染拆成可让步的批次。
2. 变电 IFC 解析继续采用当前增量 builder；后续按单 IFC 限制 raw record 生命周期，必要时引入 streaming STEP parser，避免 `scanIfcEntities` 为大型 IFC 建立过大的临时 `RawIfcEntity[]`。
3. `serializeDevToGlb` 按既定方案移入一个 Worker；`yieldToMain()` 只能改善调度，不能消除单个复杂 DEV 的长任务。

### 3.3 Fragments Cache 是否值得默认打开？

本轮不能作出“已验证可默认打开”的结论，原因不是缓存设计无效，而是采集环境未覆盖它的关键路径：

| 指标 | 本轮状态 |
|---|---|
| 首开 IFC → Fragments 转换耗时 | 未测（未启动真实 WebView Fragments 管线） |
| 二开 `.frag` 直接加载耗时 | 未测 |
| 缓存命中率/失败率/回退次数 | 未测 |
| 同路径 GIM SHA 变化失效 | 代码已有版本/SHA 条件，未做运行时样本对照 |
| 截断 `.frag` 自愈 | 代码已有删除并回退逻辑，未做真实文件故障注入 |
| Fragments/Web-IFC 版本升级失效 | 代码已有复合版本键，未做双版本运行对照 |

因此建议继续保持当前灰度策略：先在真实 Tauri 中补齐首开/二开、命中/失败/回退指标；当二开稳定命中且旧缓存不污染新工程后，再将 `ENABLE_FRAGMENTS_CACHE_BASE` 提升为默认值。

## 4. GLTFExporter 探针

使用真实 substation01 的最小可达 DEV（`DEV/001ef588-3c9f-4441-b8d6-b36ffe0746d2.dev`）在独立 Node/jsdom 子进程调用现有 `serializeDevToGlb`。探针已完成 `1 MOD + 0 STL 合并完成`，但 10 秒内没有收到 GLTFExporter 回调，父进程随后终止。

这不是有效的 exporter 时长，也不能推断生产 WebView 一定失败；它只证明 Node/jsdom 环境不适合作为当前 GLTFExporter 的可靠基准。应在真实 Tauri WebView 或 Worker 中加入开始/结束/失败埋点后再采集代表性 DEV 的 exporter 时长和 GLB 字节数。

## 5. 未测项与复测方案

以下指标本轮仍未取得完整真实值：

- Fragments Cache 首开/二开命中率、失败率、删除/回退次数（本轮线路样本不含 IFC）；
- 生产 WebView 中的 GLTFExporter 单次时长；
- Rust 解压阶段的资源级峰值（本轮只有 Tauri 进程树工作集峰值，没有单独的 Rust allocator 计数）。

`invokeTimed`、Long Task observer 和线路 Worker 已在开发版接入并完成本轮灰度；下一轮可在同一 Tauri 窗口继续：

1. 首开 A：记录 Rust 解压、manifest、每类 IPC、解析、IFC/Fragments、GLB 和 Long Task。
2. 二开 A：确认 IFC 未被读取、`.frag` 命中和加载失败回退。
3. 修改同路径 GIM 内容后再次打开：确认 SHA 失效且不复用旧模型。
4. 快速 A→B 切换：确认旧 IPC/IFC/Fragments/空间索引结果不提交到 B。
5. 每个场景至少重复三次，以中位数和 P95 形成发布门槛。

## 6. 本轮结论

1. **P0 session 提交竞态已修复并有回归测试覆盖**；CBM、FileDevRelation、空间索引和 STD/SLD 均遵守“计算局部 result → session 校验 → 同步提交”规则，线路 Worker 结果和输入批次也不会在工程切换后提交。
2. **线路解析 CPU 瓶颈已从 UI 流程中隔离**：六个真实线路样本的冷启动均 `worker:true`，Worker 中位 47–495 ms；节点、引用、FAM/DEV 属性和跨越物统计在冷/热重复中一致。
3. **线路输入读取已批量化并完成真实 IPC 复测**：六样本共 18 次冷启动的 batch 次数为 6–90、`read_cached_entry=0`、failures=0；但 line02 的 batch 累计等待约 24.9 s，下一轮应优化批次/入库和地图长任务。
4. **变电 IFC 仍是独立的内存/语义索引问题**：本轮没有做 IFC Semantic Worker 或 Spatial Index 重构，继续保持范围边界。
5. **Fragments Cache 暂保持灰度**：缓存键和损坏回退逻辑已经具备，是否默认开启必须由真实 Tauri 首开/二开、命中率、失败率和回退数据决定。
