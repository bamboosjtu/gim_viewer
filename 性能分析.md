# GIM Viewer 线路工程性能分析

> 更新日期：2026-09-01。本文记录本轮 Performance Infrastructure、Line Parser Worker v1、线路批量读取、Canvas 渐进绘制以及冷启动解压优化的实现和真实证据。
>
> **当前版本证据口径**：semantic pack 接入前，六个真实线路样本各执行 1 次冷启动、1 次缓存命中启动，共 12 次真实 Tauri/CDP 运行；batch 和 catenary A/B 各为 line02 单次对照。semantic pack 接入后另有 line02 冷/热各 1 次（`tmp/tauri-perf-semantic-line02-20260901/`）。`tmp/tauri-perf-20260901/` 中的 36 次（六样本 × 冷/热 × 3）是**旧实现历史基线**，不能与当前版本混写成 n=3 结论；semantic pack 的 line02 结果也不能冒充六样本 n=3 结果。

## 1. 结论摘要

1. **冷启动最大成本已被定位为 Rust 解压，但瓶颈随样本分化。** semantic pack 接入前 line01/02/05 的 Rust 解压分别为 27.9/56.0/33.9 s，其中写盘为 22.1/44.4/25.6 s；line03 解码（21.0 s）和写盘（27.4 s）均较大。最新 line02 profile 中解压约 4.76 s，decode 3.16 s、写盘 0.39 s，说明 staging 已压低文件提交开销，后续应按样本决定优先优化 7z decode 还是文件系统。原生解压现在使用 staging 目录，全部条目成功后再做目录级提交，避免每个文件 `sync_all + rename`。
2. **线路语义解析已移出 UI 主线程。** 六个当前冷启动均为 `worker:true`，Worker 核心耗时 65–615 ms；未再出现旧的 20–34 s 单段地图长任务。
3. **线路文本读取已从批量读取推进到 semantic pack。** semantic pack 接入前的六样本冷启动 batch IPC 为 2–27 批，`read_cached_entry.count=0`、失败数为 0；默认上限为 1024 文件/8 MiB。semantic pack 接入后的 line02 冷启动只需 1 次 `read_line_semantic_pack`，读取 22,496 个条目、22.96 MB，WebView 侧 427 ms，Rust 内部 193 ms；大 MOD/STL 仍保留为独立文件，不复制进 pack。
4. **线路地图已采用首帧 + 可取消分帧补绘。** 当前六个冷启动的导线补绘约 35–575 ms，缓存命中约 49–612 ms；热启动完整补绘约 1.2–6.8 s，主要与 WebView 调度和样本规模相关。
5. **Fragments Cache 不在本轮默认开启。** 本轮样本是线路工程，没有 IFC→Fragments 的首开/二开命中、失败和回退数据；保持实验开关关闭。

## 2. 本轮实现边界与 P1 状态

| 项目 | 状态 | 实现/证据 |
|---|---|---|
| P1-1 渲染 span、debug audit、catenary A/B | 已实现；A/B 初测 | `renderLineProjectPanels` 拆分导航、DOM、地图阶段；审计 payload 改为按需构建；catenary 通过 DEV 覆盖做 A/B。 |
| P1-2 地图/导航瓶颈、progressive/runtime index | 已实现 | Canvas 首帧与塔位/导线/跨越物/标签分帧；共享端点的运行时投影索引；视图变化合并重绘并可取消。 |
| P1-3 Rust 解压 profiling | 已实现 | manifest 携带 `header/decode/write/manifest/commit` 以及条目数、总字节、最大条目耗时。 |
| P1-4 staging directory + directory-level commit | 已实现 | staging 写入器只做目录级安全检查，成功后目录替换正式缓存；失败删除 staging，不让半成品命中。 |
| P1-5 Rust batch 内部分段计时 + 256/512/1024 A/B | 实现完成；初测完成 | `batch_read_cached_files` v2 返回 read/resolve/encode/total；line02 三档各 1 次，见第 5 节。 |
| P1-6 semantic pack | 已实现；line02 已实测 | native 线路解压将 CBM/DEV/FAM/PHM 与小型 MOD 连续写入 `.gim-line-semantic.pack`，旁置大小写不敏感索引；首开按索引区间单次打开、分段读取，单次响应与总读取均受 512 MiB/8 GiB 上限约束；旧缓存/损坏 pack 按条目回退。 |
| P1-7 SQLite 移出 ready critical path | 已实现 | 线路首帧/可交互标记后以后台任务入库，仍以 `save_line_project_finish` 作为有效缓存提交点。 |
| P2 Compact Runtime Cache / 属性 lazy load | 未开始 | 等线路输入和地图稳定后再做。 |
| IFC Semantic Worker / DEV→GLB Worker | 本轮明确不做 | 保持任务范围边界。 |

## 3. 采集方法与指标口径

### 3.1 采集目录

- 当前冷启动：`tmp/tauri-perf-current-cold-20260901/`
- 当前缓存命中：`tmp/tauri-perf-current-warm-20260901/`
- semantic pack line02 冷/热：`tmp/tauri-perf-semantic-line02-20260901/`
- batch A/B：`tmp/tauri-perf-ab-line02-20260901/`
- catenary A/B：`tmp/tauri-perf-ab-line02-catenary-20260901/`
- 历史 36 次基线：`tmp/tauri-perf-20260901/`

采集脚本为 `tmp/collect-tauri-perf.ps1`，汇总脚本为 `tmp/summarize-tauri-perf.ps1`。原始诊断保留在各目录的 `*.full.json` 和 `raw/` 中。

### 3.2 关键口径

- `interactive`：线路面板已提交、可操作的时间；不把后续 Canvas 补绘或后台 SQLite 入库混入 ready。
- `canvasFirst`：Canvas 背景/网格/图例首帧完成时间。
- `mapComplete`：最后一个渐进标签阶段完成时间；不是首帧时间。
- Rust 解压 profile：`archive` 包含归档解码和 sink 写盘；`decode`、`write` 是其中累计阶段；`manifest` 为清单/二进制 envelope 组装；`commit` 为 staging 目录提交。
- `batch total/p50/p95/max`：WebView 看到的每批 `batch_read_cached_files` 调用；semantic pack 也复用同一套 Rust envelope/profile，但单独由 `read_line_semantic_pack` 计数；`Rust read/resolve/encode` 是后端内部阶段，不能直接当作 IPC 往返时间。
- `invokeTimed` 对 `ArrayBuffer`/`Uint8Array`/字符串精确计字节；普通对象默认 `bytesMeasured=false`，不会为探针额外 `JSON.stringify`。线路 graph 已复用调用方预先计算的 JSON 字节数。
- Long Task 使用 WebView `PerformanceObserver`，阻塞时间定义为 `max(0, duration - 50 ms)`。
- RSS 是 Tauri 进程树工作集峰值，不等同 JS heap 或单进程 RSS。

## 4. semantic pack 接入前的六样本冷启动基线

每个样本 n=1；数字为该次观测，不是发布 SLA。此表用于和上一轮 batch/Canvas 结果对照，不能代表 semantic pack 接入后的六样本矩阵。

| 样本 | interactive / canvasFirst / mapComplete (ms) | Rust 解压：total / archive / decode / write / manifest / commit (ms) | 输入 (ms) | Worker / Worker ms | batch：次数 / total / p95 (ms) | SQLite 后台 (ms) | 导线补绘 (ms) | Long Task：次数 / blocking / max (ms) | 峰值工作集 (MB) | 语义 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|:---:|
| line01 | 36,837 / 38,595 / 38,906 | 27,928 / 27,425 / 4,426 / 22,102 / 462 / 38 | 7,111 | true / 380 | 14 / 7,000 / 689 | 9,964 | 260 | 18 / 1,322 / 606 | 1,453.9 | 一致 |
| line02 | 71,477 / 75,372 / 76,002 | 55,992 / 55,156 / 9,030 / 44,438 / 789 / 45 | 12,340 | true / 615 | 27 / 12,158 / 569 | 17,423 | 575 | 30 / 2,668 / 1,279 | 2,134.5 | 一致 |
| line03 | 58,610 / 60,613 / 61,068 | 50,188 / 49,982 / 20,958 / 27,389 / 186 / 19 | 7,118 | true / 317 | 21 / 7,012 / 458 | 5,677 | 292 | 11 / 1,413 / 650 | 1,655.2 | 一致 |
| line04 | 3,658 / 3,972 / 4,249 | 2,398 / 2,364 / 802 / 1,487 / 27 / 5 | 932 | true / 65 | 2 / 909 / 678 | 1,571 | 35 | 2 / 83 / 108 | 988.1 | 一致 |
| line05 | 43,624 / 45,350 / 45,734 | 33,884 / 33,394 / 6,761 / 25,630 / 455 / 33 | 7,798 | true / 387 | 19 / 7,676 / 715 | 11,114 | 330 | 20 / 1,785 / 648 | 1,658.4 | 一致 |
| line06 | 7,377 / 8,030 / 8,132 | 5,380 / 5,294 / 1,839 / 3,318 / 54 / 30 | 1,544 | true / 111 | 4 / 1,513 / 630 | 1,321 | 56 | 6 / 260 / 133 | 1,054.8 | 一致 |

### 4.1 解压分段判断

- line01：写盘约占 Rust 总耗时 79%；line02 约 79%；line05 约 76%；line06 约 62%。这些样本优先受 Windows 文件创建、目录元数据和安全软件扫描影响，而不是 7z 解码。
- line03：decode 约 21.0 s、write 约 27.4 s，二者都需要关注；不能只归因于文件 open。
- line04：总量小，write 约 1.5 s、decode 约 0.8 s，profile 仍显示目录级写入有固定成本。
- `commit` 仅约 5–45 ms，staging 目录提交没有成为瓶颈；相较逐文件同步/替换，当前方案已经把可避免的提交开销移出条目循环。

### 4.2 semantic pack 接入后的 line02 冷启动实测

该次运行是当前实现的真实 Tauri/CDP 冷启动（n=1），原始证据为 `tmp/tauri-perf-semantic-line02-20260901/line02_cold_1.full.json`。这里把“可交互”和“后台任务最终结束”分开，避免把 SQLite 后台入库误认为首屏 ready：

| 指标 | 结果 |
|---|---:|
| 线路工程可交互 | 7,041 ms |
| Canvas 首帧 | 7,039 ms |
| Canvas 地图绘制完成 | 9,881 ms |
| session 总时长（含后台 SQLite） | 20,643 ms |
| Rust 解压 command span | 4,973 ms |
| Rust profile：total / archive | 4,758 / 4,471 ms |
| Rust profile：decode / write / manifest / commit | 3,161 / 395 / 77 / 1.4 ms |
| 写盘拆分：open / data | 158 / 39 ms |
| 解压条目 / 解压后字节 | 22,640 / 96.5 MB |
| semantic pack 条目 / 大小 | 22,496 / 22.96 MB |
| 线路输入 | 572 ms |
| `read_line_semantic_pack` | 1 次；WebView 427 ms，Rust 内部 193 ms |
| Worker | `worker:true`；总 296 ms（graph 115 ms、attributes 124 ms） |
| 普通 batch / `read_cached_entry` | 0 / 0 |
| SQLite 入库 | 12,941 ms，后台执行，不阻塞可交互标记 |
| Long Task | 31 次；blocking 2,054 ms；max 720 ms |
| Tauri 进程树峰值工作集 | 1,752.8 MB |

这组数据回答了本轮遗漏点：冷启动解压不是未计量的黑盒，当前约 4.97 s；Rust 内部主要仍是 7z/归档 decode（3.16 s），而不是 staging 写盘（0.39 s）。当前 session 总时长明显高于可交互时间，主要差额来自后台 SQLite 入库和渐进地图补绘。

## 5. semantic pack 接入前的六样本缓存命中基线

缓存命中不读取原始 GIM，不启用 Worker，不执行线路 batch 读取。此表是 semantic pack 接入前的六样本基线。

| 样本 | interactive / canvasFirst / mapComplete (ms) | 导线补绘 (ms) | Long Task：次数 / blocking / max (ms) | 峰值工作集 (MB) | 语义 |
|---|---:|---:|---:|---:|:---:|
| line01 | 3,948 / 4,517 / 4,848 | 277 | 6 / 779 / 481 | 1,083.6 | 一致 |
| line02 | 5,970 / 6,084 / 6,750 | 612 | 4 / 410 / 236 | 1,211.8 | 一致 |
| line03 | 2,550 / 2,867 / 3,323 | 296 | 4 / 285 / 227 | 1,355.5 | 一致 |
| line04 | 608 / 1,129 / 1,232 | 49 | 1 / 323 / 373 | 1,342.3 | 一致 |
| line05 | 4,060 / 4,153 / 4,540 | 332 | 3 / 285 / 172 | 1,104.9 | 一致 |
| line06 | 958 / 1,086 / 1,204 | 64 | 1 / 354 / 404 | 1,104.0 | 一致 |

旧基线的 line01/02/03 热启动地图长任务约 7.6/24.7/19.3 s；当前版本通过首帧、分帧补绘和 MapLibre 视图变化合并，已不再出现同量级单段地图长任务。当前剩余的最大 Long Task 为 481/236/227 ms（对应上述三个样本）。

### 5.1 semantic pack 接入后的 line02 缓存命中实测

原始证据为 `tmp/tauri-perf-semantic-line02-20260901/line02_warm_1.full.json`（n=1）。该路径验证 semantic pack 不会污染二开：缓存命中不读取 pack，也不执行 batch；当前可见成本转移到 SQLite 恢复。

| 指标 | 结果 |
|---|---:|
| session 总时长 | 8,990 ms |
| 缓存校验 | 219 ms |
| 线路图恢复 | 1,451 ms |
| 线路属性恢复 | 3,515 ms |
| 线路面板与地图渲染 | 227 ms |
| Long Task | 5 次；blocking 472 ms；max 271 ms |
| Tauri 进程树峰值工作集 | 1,056.8 MB |
| `read_line_semantic_pack` / `batch_read_cached_files` | 0 / 0 |

因此下一轮若只优化线路 warm startup，应优先处理 SQLite 图/属性恢复或设计 Compact Line Runtime Cache，而不是继续调 semantic pack 的 batch 大小。

## 6. Batch 读取 A/B（line02，冷启动，各 1 次）

| 批次上限 | interactive (ms) | 输入 (ms) | batch 次数 | batch total / p95 (ms) | Worker (ms) | Long Task：次数 / blocking / max (ms) | 峰值工作集 (MB) |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 256 文件 / 8 MiB | 48,006 | 9,493 | 90 | 9,341 / 142 | 501 | 31 / 3,074 / 1,211 | 2,032.9 |
| 512 文件 / 8 MiB | 57,776 | 10,205 | 48 | 10,045 / 252 | 475 | 29 / 2,360 / 1,206 | 2,033.2 |
| 1024 文件 / 8 MiB | 57,866 | 9,866 | 27 | 9,733 / 460 | 507 | 29 / 2,171 / 916 | 2,060.9 |

结论：1024 将 IPC 次数从 90 降到 27，累计输入时间没有显示出稳定劣化，故保留 1024/8 MiB。单次 A/B 不能证明 1024 在所有机器上最快；若后续 p95 受大批次拖慢，再用固定机器重复至少 3 轮。三档均 `read_cached_entry=0`，语义统计一致。

## 7. Catenary A/B（line02，缓存命中，各 1 次）

| 悬链线 | interactive (ms) | mapComplete (ms) | 导线补绘 (ms) | Long Task：次数 / blocking / max (ms) | 峰值工作集 (MB) |
|:---:|---:|---:|---:|---:|---:|
| false | 5,694 | 6,623 | 608 | 6 / 508 / 224 | 1,298.7 |
| true | 6,077 | 7,307 | 614 | 6 / 914 / 440 | 1,146.2 |

一次对照中导线阶段相差约 6 ms，远小于工程解压、输入和缓存恢复时间；悬链线不是当前冷启动主瓶颈。该结果仍需多轮重复后才能形成稳定性能门槛。

## 8. 是否存在 lazy read / 主线程瓶颈？

### 8.1 磁盘 lazy read 与 IPC

semantic pack 接入前，六样本冷启动均没有逐文件 `read_cached_entry` 回退，batch 路径工作正常；line02 的 batch WebView 累计约 9.7–12.2 s，后端内部 `resolve` 约 3.1–6.8 s、`read` 约 2.4–4.2 s、`encode` 仅几十毫秒。因此当时输入阶段的主要成本是路径解析/文件读取和 IPC 等待，而不是 envelope 编码。

semantic pack 接入后的 line02 冷启动把 22,496 个语义条目合并为 1 次读取：WebView 427 ms，Rust 内部 193 ms（read 172 ms、resolve 47 ms、encode 20 ms），`read_cached_entry=0`，且 pack 采用“单次打开文件、按索引区间读取”，没有把整个 pack 再复制到 Rust 内存。这个结果支持继续保留 semantic pack；仍不引入小文件 LRU，也不把 512 MiB 上限以下的 pack 读取改成无界 `read`。

### 8.2 主线程

- Worker 核心只有 65–615 ms，已不是 UI 主线程主要来源。
- 当前地图阶段的单个导线批次在六样本基线中低于约 0.6 s；semantic pack line02 冷启动最后一轮导线补绘为约 615 ms。导航索引约 4–122 ms，导航树和摘要 DOM 约 0–3 ms。
- 冷启动可交互时间仍主要受解压和输入影响；SQLite 后台入库虽然会产生 Long Task，但已不再阻塞“线路工程可交互”标记。
- semantic pack line02 冷启动中，解压约 4.97 s、输入约 0.57 s，首屏可交互约 7.04 s；缓存命中则图/属性 SQLite 恢复约 4.97 s，是比地图绘制更大的可见成本。后续应优先验证 Compact Line Runtime Cache/属性 lazy load，再决定是否细化导线批次。

### 8.3 内存

semantic pack 接入前六样本冷启动进程树峰值约 988–2,135 MB，缓存命中约 1,084–1,356 MB；line02 semantic pack 冷/热分别为 1,752.8/1,056.8 MB。该数据是工作集采样，不能替代 JS heap；下一轮若要制定门槛，应固定机器、重复运行并单独采集 WebView/Rust 进程。

## 9. 语义与回归验证

- 当前六个冷启动均报告 `worker:true`、`read_cached_entry.count=0`、batch failures=0。
- semantic pack line02 冷启动报告 `worker:true`、`semanticPackReads=1`、`read_line_semantic_pack.count=1`、`read_cached_entry.count=0`、batch failures=0；缓存命中报告 pack/batch 均为 0。
- 六个冷启动和六个缓存命中的诊断均为 `project_type=transmission_line`，FAM/DEV 缺失源为 0，节点/引用/FAM/DEV 统计与各自缓存恢复结果一致；跨越物统计保持不变。
- semantic pack line02 冷启动/缓存命中：解析结果、导航树、属性、地图数据与 semantic pack 前一致，仍为 9,276 CBM 节点、129 杆塔、2,232 导线段、44 个跨越物；FAM/DEV 未匹配均为 0。
- `npm test -- --run`：53 个测试文件、596 个测试通过。
- `npm run test:sample`：4 个样本回归测试文件、22 个测试通过，耗时约 208 s；line02 图、属性、地图数据链路统计保持 129 杆塔、2,232 导线段、44 跨越物。
- MapLibre/Canvas 渐进绘制回归：`lineMapView.test.ts` 3 个测试通过，覆盖首帧、共享端点投影、销毁取消和连续视图变化。
- `npm run build`：TypeScript 与 Vite 构建通过；仅保留既有的大 chunk 提示。
- `cargo check --manifest-path desktop/src-tauri/Cargo.toml`：通过。
- `cargo test --manifest-path desktop/src-tauri/Cargo.toml`：20 个测试通过。

## 10. 下一轮决策

1. 在固定 Tauri 机器上把 semantic pack 冷/热矩阵扩展到六个线路样本、每个场景至少 n=3，用中位数和 P95 验证 pack 读取、解压 decode 与工作集上限。
2. Rust profile 已显示 line02 写盘/commit 不是主耗时；若其他样本仍由 `decode` 主导，优先做 7z 解码/线程与归档结构实验；若 `write/resolve` 在多轮中重新占主导，再做 Windows 文件系统/安全软件环境下的 staging 对照。
3. semantic pack 已落地，继续保持 source SHA、`gim-parser-v20` 与索引/格式绑定；损坏或旧 pack 维持按条目回退，不引入无界读取。
4. semantic pack line02 热启动的图/属性恢复约 4.97 s，下一轮优先设计 Compact Line Runtime Cache 或属性 lazy load；只有确认地图补绘重新成为主导时，才细化导线批次。
5. IFC/Fragments 仍需独立的真实 Tauri 首开/二开、命中率、失败率、回退和 GLTFExporter 数据；在这些证据出现前，不把 Fragments Cache 改为默认能力，也不扩大到 IFC Semantic Worker。

## 11. 历史数据说明

`tmp/tauri-perf-20260901/summary.md` 的 36 次数据属于 progressive Canvas 和 Rust profile 完整接入前的历史基线，其中 ready 定义也曾把地图同步绘制结束点混入。它适合用来说明旧的 7–34 s 地图长任务已被消除，不适合与本报告当前表格直接做严格倍率比较。
