# GIM Viewer 线路工程性能分析（Line Warm Fast Path v1）

> 更新日期：2026-09-02。范围仅为线路工程 semantic pack 完整性、热启动 fast path 与真实性能复测；未修改变电/IFC、Fragments Cache、Compact Line Runtime Cache、属性 lazy load 或 7z 解码器。
>
> 本轮使用真实 Tauri Dev WebView（CDP `127.0.0.1:9225`），样本 `line01`–`line06`，cold/warm 各 3 次，共 36 次。原始诊断在 [`tmp/tauri-perf-20260902-v1`](D:/vibe-coding/gim_viewer/tmp/tauri-perf-20260902-v1)；汇总由 [`tmp/summarize-tauri-perf.ps1`](D:/vibe-coding/gim_viewer/tmp/summarize-tauri-perf.ps1) 生成。

## 1. 结论先行

1. semantic warm fast path 36 次中 warm **18/18 命中**，SQLite fallback **0 次**；每次只调用一次 `read_line_semantic_pack_all`，然后复用 Line Parser Worker 重建图和 FAM/DEV 结果。
2. warm 首次可交互中位数为 **298–2,531 ms**，地图完整补绘中位数为 **511–3,324 ms**，Worker 为 **28–317 ms**。线路解析不再是 UI 主线程的主要瓶颈。
3. cold 仍由原生解压主导，Rust profile 的 decode 中位数为 **468–13,098 ms**；最大的是 line03。staging 写盘仅 **70–424 ms**（Rust 内部），当前无需继续改写目录提交逻辑；若需要降低 cold SLA，应另行研究 7z decode。
4. semantic pack 的 Rust full-read 总耗时中位数为 **42–572 ms**，其中实际 `readMs` 只有 **3–49 ms**；`readMs` 已与 index/resolve/encode 互斥，不再包含路径解析。
5. 进程树工作集 warm 峰值中位数约 **912–1,295 MB**，仍偏高且本次在同一 Tauri 进程中连续切换样本，不能当作 JS heap。启动架构本轮可以冻结；下一阶段若需继续优化，优先进入 Compact Line Runtime Cache + Full Property Lazy Load，并用独立进程复测 RSS。

## 2. semantic pack 完整性与降级语义

### 2.1 格式与校验

- parser 版本已升级为 `gim-parser-v21`。
- index 为 `GLSI` v2：每条记录保存原始路径、offset、size 和 `packed` 标志；metadata-only 的大 MOD/STL 保留大小但不复制进 pack。v1 index 仍可读，旧条目按 packed 处理。
- full-read 为 `GIMF` v1 envelope，由 Rust 直接按 index 返回完整条目，WebView 不再传递两万条路径。
- 路径统一把 `\` 转为 `/` 后做大小写不敏感查找；拒绝绝对路径、`..`、重复归一化路径和未知 flags。
- Rust 共享校验：index 头/长度/UTF-8/重复路径、packed offset/size、区间重叠/空洞/尾部、pack 上限，以及 metadata-only 文件存在性和大小。
- 空 index 或不含 packed CBM 入口一律 `INDEX_INVALID`，避免“两个空 sidecar 看似有效”时提交空图。

### 2.2 错误分类和动作

| 错误 | 含义 | 允许的动作 |
|---|---|---|
| `ENTRY_MISS` | 单次旧式按路径读取中，某一个请求未命中 | 仅该条 `read_cached_entry`/普通 File 读取回退；其它条目可继续 |
| `PACK_INVALID` | pack 缺失、格式/大小/metadata 校验失败或响应不一致 | semantic source cache 整体失效，重新解压/重建；禁止 partial Runtime |
| `INDEX_INVALID` | index 缺失、截断、重复/非法路径、错误 flags、空/无 CBM 入口 | 同上，禁止逐条 fallback 后提交 |
| `PACK_TRUNCATED` | offset 溢出、区间越界或 `read_exact` 不足 | 同上，禁止 partial Runtime |

两个 sidecar 都不存在时状态为 `missing`，兼容历史 SQLite-only cache；此时允许 SQLite graph/attribute restore。只缺一边、index/pack 损坏或 metadata-only 文件不匹配时状态为 `invalid`，`validate_gim_cache` 不再把工程视为可命中缓存，打开流程重新解压。校验后到 full-read 之间若文件被删除/截断，仍按整体错误抛出，由外层走完整解压；不会清空或覆盖另一个工程的 state。

## 3. 真实性能采集方法

- 每个样本 cold 3 次：删除该项目缓存后，走 `get_file_info → validate → Rust 解压 → semantic pack 输入 → Worker → 地图`。
- 每个样本 warm 3 次：缓存校验通过，优先 `read_line_semantic_pack_all → Worker`；只有 semantic source `missing` 才允许旧 SQLite fallback。
- `interactive` 是“线路工程可交互” mark；`mapComplete` 是最后一批 Canvas 补绘完成，不将后台 SQLite 入库混入 interactive。
- Rust 解压 profile 分为 `archive/decode/write/manifest/commit`；full-read profile 分为 `index/resolve/read/encode/total`。`readMs` 只包含 seek/read。
- Long Task 使用 WebView `PerformanceObserver`，blocking time 为 `max(0, duration−50ms)`。
- RSS 是 Tauri 进程树工作集每 250 ms 采样的峰值，不等于 JS heap；同一进程连续运行会保留 allocator/浏览器工作集，数值仅用于本批次比较。

## 4. 六样本 cold/warm 结果

时间单位为 ms；每个单元格为该样本/模式 3 次观测的 `median / P95`。`—` 表示该路径未执行。

| 样本 | 模式 | interactive | 解压 Rust total / decode / write | semantic pack full-read | Worker | mapComplete | Long Task count / blocking / max | RSS peak median / max (MB) | fast path | SQLite fallback | 语义一致 |
|---|---|---:|---:|---:|---:|---:|---:|---:|:---:|---:|:---:|
| line01 | cold | 4,274 / 4,710 | 2,953 / 1,935 / 270 | — | 190 | 5,868 / 6,156 | 16 / 917 / 413 | 1,277.2 / 1,369.9 | 0/3 | 0 | 是 |
| line01 | warm | 1,498 / 1,625 | — | 492 / 517 | 176 | 1,946 / 2,066 | 3 / 161 / 137 | 1,111.2 / 1,113.7 | 3/3 | 0 | 是 |
| line02 | cold | 7,884 / 9,039 | 5,404 / 3,572 / 424 | — | 350 | 11,052 / 12,564 | 32 / 2,438 / 832 | 1,966.5 / 2,000.2 | 0/3 | 0 | 是 |
| line02 | warm | 2,531 / 2,584 | — | 842 / 846 | 317 | 3,324 / 3,393 | 5 / 392 / 209 | 1,295.4 / 1,298.3 | 3/3 | 0 | 是 |
| line03 | cold | 16,626 / 16,993 | 15,113 / 13,098 / 358 | — | 268 | 18,978 / 19,505 | 12 / 1,007 / 680 | 1,580.3 / 1,614.1 | 0/3 | 0 | 是 |
| line03 | warm | 1,577 / 2,328 | — | 534 / 761 | 236 | 2,150 / 2,905 | 3 / 106 / 115 | 1,098.3 / 1,273.8 | 3/3 | 0 | 是 |
| line04 | cold | 921 / 947 | 648 / 468 / 70 | — | 30 | 1,422 / 1,696 | 2 / 22 / 69 | 992.7 / 1,011.2 | 0/3 | 0 | 是 |
| line04 | warm | 298 / 309 | — | 78 / 80 | 28 | 511 / 765 | 0 / 0 / 0 | 912.3 / 949.3 | 3/3 | 0 | 是 |
| line05 | cold | 5,678 / 6,207 | 3,898 / 2,572 / 377 | — | 249 | 7,702 / 8,143 | 20 / 1,075 / 557 | 1,534.8 / 1,555.5 | 0/3 | 0 | 是 |
| line05 | warm | 1,740 / 1,883 | — | 594 / 671 | 219 | 2,209 / 2,346 | 3 / 185 / 145 | 1,179.5 / 1,214.1 | 3/3 | 0 | 是 |
| line06 | cold | 1,322 / 1,334 | 1,013 / 728 / 120 | — | 35 | 1,851 / 1,855 | 3 / 68 / 83 | 1,100.5 / 1,166.8 | 0/3 | 0 | 是 |
| line06 | warm | 335 / 352 | — | 96 / 97 | 39 | 563 / 575 | 0 / 0 / 0 | 951.3 / 955.1 | 3/3 | 0 | 是 |

### 4.1 semantic pack 内部阶段（warm，中位数）

| 样本 | index / resolve / read / encode / total | pack packed bytes | index entries / packed entries |
|---|---:|---:|---:|
| line01 | 57.5 / 202.5 / 26.5 / 11.0 / 304.7 | 14.90 MB | 12,372 / 12,273 |
| line02 | 100.1 / 388.8 / 49.1 / 21.6 / 572.4 | 22.96 MB | 22,640 / 22,496 |
| line03 | 76.0 / 307.1 / 33.9 / 9.5 / 432.6 | 5.97 MB | 17,761 / 17,654 |
| line04 | 15.3 / 20.4 / 3.4 / 1.9 / 42.5 | 2.59 MB | 1,394 / 1,351 |
| line05 | 67.7 / 275.2 / 32.8 / 13.1 / 399.0 | 16.73 MB | 15,480 / 15,341 |
| line06 | 20.3 / 34.1 / 5.3 / 1.8 / 65.4 | 1.99 MB | 2,508 / 2,452 |

`read_line_semantic_pack_all` 的 WebView invoke 响应包含 envelope 开销，实际观测为 line01/02/03/04/05/06 中位约 **490/818/523/78/594/96 ms**；Rust `readMs` 仍仅为 **26.5/49.1/33.9/3.4/32.8/5.3 ms**。因此当前主要不是磁盘 seek/read，而是 index resolve、envelope/IPC 和 WebView 解析/Worker 输入准备。

### 4.2 语义结果一致性

所有 36 条诊断均为 `project_type=transmission_line`、FAM/DEV 缺失源为 0。每个样本的关键计数在 cold/warm 之间保持一致：

| 样本 | CBM 节点 | CBM refs | FAM 属性 | DEV 属性 | 杆塔 | 导线段 | 跨越物 |
|---|---:|---:|---:|---:|---:|---:|---:|
| line01 | 4,998 | 30,339 | 22,327 | 88,404 | 40 | 1,013 | 19 |
| line02 | 9,276 | 56,018 | 45,726 | 123,242 | 129 | 2,232 | 44 |
| line03 | 7,949 | 45,831 | 35,934 | 10,374 | 111 | 1,081 | 772 |
| line04 | 499 | 3,062 | 2,831 | 16,064 | 6 | 138 | 4 |
| line05 | 5,447 | 33,098 | 28,165 | 97,398 | 49 | 1,154 | 23 |
| line06 | 978 | 5,934 | 4,707 | 10,138 | 14 | 198 | 10 |

这些计数由同一套 Line Parser Worker、导航树和 `extractLineMapData` 路径产生，覆盖 graph、导航树、塔位、导线、跨越物以及 FAM/DEV 属性来源追溯。line02 还通过样本回归单独核对了完整属性结果（129 杆塔、2,232 导线段、44 跨越物）。

## 5. IPC、主线程和冷启动判断

### 5.1 IPC

- 36 次中 `batch_read_cached_files.count=0`、`read_cached_entry.count=0`；六个样本都成功使用 native semantic pack，因此本批次没有触发单文件 fallback。
- cold 每次有一次旧式 `read_line_semantic_pack`，输入阶段为 **61–744 ms**；warm 每次有一次 `read_line_semantic_pack_all`，full-read WebView span 为 **78–842 ms**。
- `invokeTimed` 对普通对象不再 `JSON.stringify`，标记 `bytesMeasured=false`；ArrayBuffer/Uint8Array 和显式 `requestBytes/responseBytes` 仍精确统计，避免性能探针本身复制 graph payload。
- 本批次没有 `batch_read_cached_files` 实际请求，不能用这 36 次推断 256/512/1024 批次的优劣；批量读取仍保留为 pack 缺失/浏览器 File 的兼容路径。

### 5.2 主线程与地图

- Worker 核心仅 28–350 ms，warm 仅 28–317 ms；解析计算已不阻塞 UI。
- warm 地图完整补绘中位数 511–3,324 ms，line02 最高但仍远低于旧版 7–25 s 级同步地图冻结。
- warm Long Task 中位 blocking 0–392 ms；cold line02 为 2,438 ms，主要包含首开后地图/后台入库期间的 WebView 任务，不代表 Worker 单段耗时。
- 线路 SQLite 入库只在 cold 首帧后后台执行；它不计入 interactive，但仍可能制造后续 Long Task，这解释了 session 总耗时明显高于 interactive 的现象。

### 5.3 冷启动解压

Rust profile 的 `decode` 中位数：line01 **1.94 s**、line02 **3.57 s**、line03 **13.10 s**、line04 **0.47 s**、line05 **2.57 s**、line06 **0.73 s**。`write` 仅 **0.07–0.42 s**，`commit` 为约 **2–10 ms** 量级；因此 line03 明确由归档 decode 主导，其他样本也是 decode 大于 staging write。当前不再扩大 staging/目录提交改造，若 cold 仍需优化，应单独做 7z decoder/归档结构实验。

## 6. 验证结果

- `npm test -- --run`：**54 个测试文件、612 个测试通过**。
- `npm run test:sample`：**4 个文件、30 个测试通过**；line02 真实样本 graph/属性/地图链路通过。
- `cargo test --manifest-path desktop/src-tauri/Cargo.toml`：**22 个通过**，包含 semantic pack v2、大小写路径、截断/缺失/错误 offset/空 index 分类。
- `cargo check --manifest-path desktop/src-tauri/Cargo.toml`：通过。
- `npm run build`：TypeScript、Vite 构建通过；仅有既有的大 chunk 提示。
- 新增/覆盖：full envelope 截断、长度不一致、非法 flags、重复大小写路径、路径穿越、空 index；semantic pack 单条 miss 与整体错误；Worker/cold/warm/SQLite 业务形状一致；A→B 切换时旧 pack/Worker 成功或失败均不能提交或清空 B。

## 7. 下一阶段决策

1. **线路 semantic-pack 启动架构本轮可以停止继续扩展。** 18/18 warm fast path 命中，语义 6/6 一致，warm interactive 已进入秒级，且没有 batch/read_cached_entry fallback。
2. **若产品目标是降低内存，下一阶段进入 `Compact Line Runtime Cache + Full Property Lazy Load`。** 当前 RSS 是进程树工作集，warm 中位仍约 0.9–1.3 GB；实施前应使用每次全新 Tauri 进程、固定 WebView 版本和独立 RSS/JS heap 采样，避免把 allocator 保留误判为 runtime 数据。
3. **若产品目标是降低 cold 首开，另立 7z decode 专项。** line03 decode 中位 13.10 s，已足够证明归档解码是主要对象；本轮不改解码器，避免扩大范围。
4. IFC/Fragments 本轮没有运行，不能据此改变 Fragments Cache 默认开关；继续保持现有灰度关闭策略。

## 8. 可复查证据

- 运行清单：[`tmp/tauri-perf-20260902-v1/manifest.json`](D:/vibe-coding/gim_viewer/tmp/tauri-perf-20260902-v1/manifest.json)
- 汇总 JSON/Markdown：[`tmp/tauri-perf-20260902-v1/summary.json`](D:/vibe-coding/gim_viewer/tmp/tauri-perf-20260902-v1/summary.json)、[`tmp/tauri-perf-20260902-v1/summary.md`](D:/vibe-coding/gim_viewer/tmp/tauri-perf-20260902-v1/summary.md)
- 采集脚本：[`tmp/collect-tauri-perf.ps1`](D:/vibe-coding/gim_viewer/tmp/collect-tauri-perf.ps1)
- 汇总脚本：[`tmp/summarize-tauri-perf.ps1`](D:/vibe-coding/gim_viewer/tmp/summarize-tauri-perf.ps1)
- semantic pack 实现：[`desktop/src-tauri/src/db.rs`](D:/vibe-coding/gim_viewer/desktop/src-tauri/src/db.rs)、[`desktop/bridge/database.ts`](D:/vibe-coding/gim_viewer/desktop/bridge/database.ts)
- warm/cold 共用提交和 fast path：[`desktop/src/services/openGimService.ts`](D:/vibe-coding/gim_viewer/desktop/src/services/openGimService.ts)
